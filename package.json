{
  "name": "@browserspy/common",
  "version": "0.1.1",
  "description": "",
  "main": "./dist/index",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "compile": "npm run -s clean && tsc",
    "lint": "tslint -c tslint.json './{src,test}/**/*.ts'",
    "postversion": "git push --tags && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/glob": "^5.0.35",
    "@types/node": "^10.5.6",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.9.2",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "express": "^4.16.3",
    "glob": "^7.1.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
